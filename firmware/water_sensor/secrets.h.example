#pragma once

// Wi-Fi (2.4 GHz only)
#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// Railway backend URL (no trailing slash)
#define SERVER_URL    "https://your-backend.railway.app"

// Device token registered via the dashboard
#define DEVICE_TOKEN  "your-device-token"
